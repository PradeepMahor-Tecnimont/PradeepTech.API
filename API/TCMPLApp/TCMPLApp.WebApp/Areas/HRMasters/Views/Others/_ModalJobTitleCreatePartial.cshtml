@model TCMPLApp.WebApp.Areas.HRMasters.Models.JobTitleCreateViewModel

@Html.AntiForgeryToken()

@{
    UserIdentity currentUserIdentity = CurrentUserIdentity;
}

<form asp-controller="Others"
      asp-action="JobTitleCreate"
      data-ajax="true"
      data-ajax-method="post"
      data-ajax-update=""
      data-ajax-begin="onBegin(null)"
      data-ajax-complete="onComplete(null)"
      data-ajax-success="onDataTableReloadSuccess"
      data-ajax-failure="onError"
      accept-charset="UTF-8">

    <div class="modal-header">
        <h5 class="modal-title">New job title</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
    </div>
    <div class="modal-body">        
        <div class="row">
            <div class="col-xl-12 col-md-12">
                <div class="form-group">
                    <label asp-for="JobtitleCode" class="control-label"></label>
                    <input asp-for="JobtitleCode" class="form-control" placeholder="Job title code" required />
                    <span asp-validation-for="JobtitleCode" class="text-danger"></span>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-xl-12 col-md-12">
                <div class="form-group">
                    <label asp-for="Jobtitle" class="control-label"></label>
                    <input asp-for="Jobtitle" class="form-control" placeholder="Job title" required />
                    <span asp-validation-for="Jobtitle" class="text-danger"></span>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        <button class="btn btn-primary" type="submit" id="btnconfirm"
                data-loading-text='<span class="spinner-border spinner-border-sm" role="status"></span>"Waiting..."'
                data-text="Confirm">
            Confirm
        </button>
        <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
    </div>
</form>

@section scripts{
    <partial name="_ValidationScriptsPartial" />
}


