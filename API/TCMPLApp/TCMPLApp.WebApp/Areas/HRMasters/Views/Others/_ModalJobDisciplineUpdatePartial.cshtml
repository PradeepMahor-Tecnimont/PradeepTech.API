@model TCMPLApp.WebApp.Areas.HRMasters.Models.JobDisciplineCreateViewModel

@Html.AntiForgeryToken()

@{
    UserIdentity currentUserIdentity = CurrentUserIdentity;
}

<form asp-controller="Others"
      asp-action="JobDisciplineEdit"
      data-ajax="true"
      data-ajax-method="post"
      data-ajax-update=""
      data-ajax-begin="onBegin(null)"
      data-ajax-complete="onComplete(null)"
      data-ajax-success="onDataTableReloadSuccess"
      data-ajax-failure="onError"
      accept-charset="UTF-8">
   
    <div class="modal-header">
        <h5 class="modal-title">Edit grade</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
    </div>
    <div class="modal-body">
        <div class="row">
            <div class="col-xl-12 col-md-12">
                <div class="form-group">
                    <label asp-for="JobdisciplineCode" class="control-label"></label>
                    <input asp-for="JobdisciplineCode" class="form-control" readonly="readonly" />
                </div>
            </div>
        </div>        
        <div class="row">
            <div class="col-xl-12 col-md-12">
                <div class="form-group">
                    <label asp-for="Jobdiscipline" class="control-label"></label>
                    <input asp-for="Jobdiscipline" class="form-control" placeholder="Description" required />
                    <span asp-validation-for="Jobdiscipline" class="text-danger"></span>
                </div>
            </div>
        </div>
    </div>

    <div class="modal-footer">
        <button class="btn btn-primary" type="submit" id="btnconfirm"
                data-loading-text='<span class="spinner-border spinner-border-sm" role="status"></span>"Waiting..."'
                data-text="Confirm">
            Confirm
        </button>
        <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
    </div>
</form>

@section scripts{
    <partial name="_ValidationScriptsPartial" />
}
