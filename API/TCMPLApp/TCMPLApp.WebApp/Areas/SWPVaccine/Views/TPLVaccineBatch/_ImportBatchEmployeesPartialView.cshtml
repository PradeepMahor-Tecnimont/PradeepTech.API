@model TCMPLApp.WebApp.Models.TPLVaccineBatchImportEmployeesViewModel

<div class="modal-header">
    <h5 class="modal-title">Import Batch Employee</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
</div>
<div class="modal-body">
    <div class="modal-content ">
        <div class="card-header app-h4 app-card-header">
            <p id="modalheader" class="mb-1">Import Batch Employees</p>
        </div>
        <div class="modal-body">

            @*<form asp-action="ImportBatchEmployees" asp-controller="TPLVaccineBatch" method="post" enctype="multipart/form-data" class="dropzone needsclick dz-clickable" id="dropzoneForm"
                  style="background: none; border: none; ">
                <div class="border">
                    <div class="dz-default needsclick dz-message box__input">
                        <i class="fa fa-cloud-upload" style="font-size:23px;position:relative;top:4px;"></i>
                        <span>Drop file here to upload</span>
                    </div>
                </div>
                <input type="hidden" value="@ViewBag.TPLVaccineBatch.BatchKeyId" name="batchId" />
            </form>*@

            @*<form asp-action="ImportBatchEmployees" asp-controller="TPLVaccineBatch" method="post" enctype="multipart/form-data" >

                <div class="form-group border">
                    <label class="required">@Html.DisplayNameFor(m=> m.FileToUpload)</label>
                    <input type="file" asp-for="FileToUpload" class="form-control-file" required/>
                    <span asp-validation-for="FileToUpload" class="danger"></span>
                </div>
                <div class="text-right"><button type="submit" class="btn btn-primary">Upload</button> </div>
            </form>*@

            <!--class="dropzone" Use dropzone's own style style in the referenced CSS -->
            <form asp-action="ImportBatchEmployees" asp-controller="TPLVaccineBatch" 
                  class="dropzone border"
                  enctype="multipart/form-data"
                  id="myDropzone"
                  method="post"
                  style="display:block;"
                  >
                <!--When uploading a picture, the data that needs to be submitted at the same time, here is a demonstration-->
                <input type="hidden" value="@ViewBag.TPLVaccineBatch.BatchKeyId" name="batchId" />
            </form>
            <div class="mt-3">
                <!--Upload button, provides the function of uploading multiple pictures at once-->
                <button type="submit" class="btn btn-outline-primary"  id="submit-all" disabled="disabled">Upload</button>
            </div>
        </div>
        <div class="modal-footer">
            <button class="btn btn-secondary" type="button" data-dismiss="modal">Close</button>
        </div>
    </div>
</div>
